<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Error - Smart EDU</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">

  <div class="container text-center py-5">
    <div class="card shadow-sm p-4 mx-auto" style="max-width: 600px;">
      <h1 class="fw-bold text-danger mb-3">⚠️ Oops! Something went wrong.</h1>
      <p class="text-muted">
        <%= err && err.message ? err.message : "An unexpected error has occurred. Please try again later." %>
      </p>

      <% if (err && err.statusCode) { %>
        <p class="badge bg-secondary">Error Code: <%= err.statusCode %></p>
      <% } %>

      <% if (process.env.NODE_ENV === "development" && err && err.stack) { %>
        <div class="text-start mt-4">
          <h5>Stack Trace (Dev Only):</h5>
          <pre class="bg-light border p-3 rounded"><%= err.stack %></pre>
        </div>
      <% } %>

      <div class="mt-4">
        <a href="/" class="btn btn-primary">⬅ Back to Home</a>
      </div>
    </div>
  </div>

</body>
</html>
